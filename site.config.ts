type SiteConfig = {
  site_domain: string;
  site_name: string;
  site_description: string;
};

export const siteConfig: SiteConfig = {
  site_name: "CTI",
  site_description: "CTI – Custom Audio Visual Solutions & Services",
  site_domain: "http://localhost:3000",
};
